# task.md — TodoFlow

Generated by `/plan`. Updated by agents during `/build`. All tasks must be `[x]` before Phase 4 exit gate passes.

---

## Group 1 — Infrastructure & Setup (complete first, blocks everything else)

- [x] `[backend]` Set up Hono app, Prisma schema (`User`, `Task`), and DB connection to Railway PostgreSQL
- [x] `[frontend]` Set up Next.js 14 project with Tailwind v4, React Query v5, and `shared/lib/api.ts` Axios instance

---

## Group 2 — Auth (backend + frontend in parallel)

- [x] `[backend]` `POST /api/v1/auth/register` — validate email/password, hash with bcrypt, create user, issue JWT httpOnly cookie, return `{ data: { id, email, role } }`
- [x] `[backend]` `POST /api/v1/auth/login` — verify credentials, issue JWT cookie, return user data. Same error for wrong email and wrong password.
- [x] `[backend]` `POST /api/v1/auth/logout` — clear JWT cookie, return `{ data: null }`
- [x] `[frontend]` Register page — `RegisterForm` with email + password fields, validation, `useRegister` hook, redirect to `/dashboard` on success
- [x] `[frontend]` Login page — `LoginForm`, `useLogin` hook, redirect to `/dashboard` on success
- [x] `[frontend]` Auth guard — middleware in Next.js `middleware.ts` to redirect unauthenticated users from `/dashboard` and `/team` to `/login`

---

## Group 3 — Task Management (backend + frontend in parallel, requires Group 2)

- [x] `[backend]` `GET /api/v1/tasks` — return current user's non-deleted tasks ordered: overdue first, then by due_date asc, undated last, done at bottom
- [x] `[backend]` `POST /api/v1/tasks` — validate title (required, ≤200 chars) + due_date (optional, not in past), create task for current user with status `TODO`
- [x] `[backend]` `PATCH /api/v1/tasks/:id/toggle` — verify ownership, toggle `TODO` ↔ `DONE`, return updated task
- [x] `[backend]` `DELETE /api/v1/tasks/:id` — verify ownership, soft-delete (`deleted_at = now()`), return `{ data: null }`
- [x] `[frontend]` Dashboard — `TaskList` with `TaskItem` components: checkbox (toggle), title, due date badge, overdue highlight, done strikethrough
- [x] `[frontend]` `TaskCreateForm` — title input (required) + optional date picker + submit. Appears at top of task list.
- [x] `[frontend]` `EmptyState` — renders "No tasks yet — add your first one" with CTA when task list is empty
- [x] `[frontend]` Task toggle — `useToggleTask` hook with optimistic update (checkbox responds instantly, reverts on API error)
- [x] `[frontend]` Task deletion — `useDeleteTask` hook with undo toast (3s window to cancel before soft-delete)

---

## Group 4 — Team Overview (requires Group 2 and Group 3)

- [x] `[backend]` `GET /api/v1/team/overview` — TEAM_LEAD only (403 for USER role). Returns all users with `{ id, email, total_tasks, done_count, overdue_count }`.
- [x] `[backend]` `GET /api/v1/team/members/:id/tasks` — TEAM_LEAD only. Returns any user's non-deleted tasks.
- [x] `[frontend]` Team overview page at `/team` — `TeamOverview` table with member rows. Each row: name, total/done/overdue counts. Clicking expands `MemberTaskList` (read-only).

---

## Group 5 — Tests (runs after Group 3 and Group 4 complete)

- [x] `[qa]` Backend: auth routes — register (happy path, duplicate email → 409, password < 8 chars → 400), login (valid credentials → 200, wrong password → 401, unknown email → 401), logout (clears cookie)
- [x] `[qa]` Backend: task routes — create (valid, missing title, past due date), list (only own tasks returned, soft-deleted excluded), toggle (ownership check), delete (soft-delete verified, other user's task → 403)
- [x] `[qa]` Backend: team routes — overview (TEAM_LEAD → 200, USER → 403), member tasks (TEAM_LEAD → 200, USER → 403)
- [x] `[qa]` Frontend: `TaskItem` — renders title, renders due date, applies overdue class when past due, applies done class when status is DONE
- [x] `[qa]` Frontend: `TaskCreateForm` — disables submit when title is empty, shows error when due date is in the past, calls `onSubmit` with correct data
- [ ] `[qa]` E2E (Playwright): register new user → add task with due date → toggle task to done → delete task → verify list is empty
  - **Note:** Blocked until dev server is stable in CI. In progress.

---

## Blocked

*(none)*

---

## Legend

- `[x]` — Complete
- `[ ]` — Pending
- `[blocked: reason]` — Cannot proceed until blocker is resolved

---

## Stats (end of build phase)

- Total tasks: 23
- Complete: 22
- In progress: 1 (E2E test — pending CI stability)
- Blocked: 0
